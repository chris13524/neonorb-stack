<VirtualHost *:80>
        ServerName bugs.${DOMAIN_NAME}
        DocumentRoot /var/www/html/bugs
RewriteEngine on
RewriteCond %{SERVER_NAME} =bugs.${DOMAIN_NAME}
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,QSA,R=permanent]
</VirtualHost>
